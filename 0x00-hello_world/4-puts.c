#include <stdio.h>
#include <string.h>
/**
 * main: Main function of my project to prints on the standard output prgramming is like building a multilingual puzzle
 *
 * Return : return 0 everything
 */
int main(void)
{
	char displayed[52];
	strcpy(displayed, "\"programming is like building a multiple puzzle");
	puts(displayed);
	return (0);
}
